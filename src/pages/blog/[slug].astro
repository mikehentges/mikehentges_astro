---
import BaseLayout from '../../layouts/BaseLayout.astro';

export function getStaticPaths() {
  return [{ params: { slug: 'placeholder' } }];
}

const { slug } = Astro.params;
---

<BaseLayout title={`Blog Post: ${slug} | Hentges.AI`}>
  <h1>Blog Post: {slug}</h1>
</BaseLayout>
